{
  "version": 2,
  "installCommand": "rm -f pnpm-lock.yaml && npm ci",
  "buildCommand": "npm run build",
  "rewrites": [{ "source": "/(.*)", "destination": "/api" }],
  "env": {
    "NODE_ENV": "production"
  }
}
